export function GuidedEntry({ onReplay, onHow, onAddSensor, onSensors, loading, canOperate = true }: { onReplay: () => void; onLive?: () => void; onHow: () => void; onAddSensor: () => void; onSensors: () => void; loading: boolean; canOperate?: boolean }) {
  return <section className="guided-entry" aria-labelledby="get-started-heading"><div className="entry-intro"><span className="overline">Start here</span><h2 id="get-started-heading">Connect a server, then review what Sentinel knows.</h2><p>The operator path begins with a real sensor. Replay remains available for product walkthroughs and is always labeled as prepared data.</p></div><div className="entry-options"><button className="entry-option entry-recommended" onClick={onAddSensor}><strong>Add sensor</strong><small>Install and register an agent, then verify heartbeat and telemetry until the sensor is online.</small><em>Primary operator path&nbsp; ↗</em></button><button className="entry-option" onClick={onSensors}><strong>Review sensors</strong><small>See registered servers, separate Agent/Telemetry/Forecast health, and open a sensor detail.</small><em>Fleet overview&nbsp; ↗</em></button>{canOperate && <button className="entry-option" onClick={onReplay} disabled={loading}><strong>{loading ? "Loading replay…" : "Use Replay"}</strong><small>Review prepared forecast output and recommendations without presenting it as live telemetry.</small><em>Secondary walkthrough&nbsp; ↗</em></button>}</div><button className="text-button guided-how" onClick={onHow}>How the local telemetry path works</button></section>;
}
