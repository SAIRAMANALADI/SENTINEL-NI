export function GuidedEntry({ onReplay, onLive, onHow, loading }: { onReplay: () => void; onLive: () => void; onHow: () => void; loading: boolean }) {
  return <section className="guided-entry" aria-labelledby="get-started-heading"><div className="entry-intro"><span className="overline">Get started</span><h2 id="get-started-heading">Start with a demo or live network traffic.</h2><p>Choose the path that matches what you want to see. Demo mode uses prepared traffic; live mode observes this machine.</p></div><div className="entry-options"><button className="entry-option entry-recommended" onClick={onReplay} disabled={loading}><strong>{loading ? "Running demo…" : "Run demo"}</strong><small>See prepared traffic become network states, a forecast, candidate sources, and recommendations.</small><em>Prepared demonstration traffic&nbsp; ↗</em></button><button className="entry-option" onClick={onLive}><strong>Live network</strong><small>Observe traffic metadata from a selected interface. Payload contents are not retained.</small><em>Start live monitoring&nbsp; ↗</em></button><button className="entry-option" onClick={onHow}><strong>How it works</strong><small>Understand how traffic becomes flows, network states, forecasts, and recommendations.</small><em>Learn about Sentinel&nbsp; ↗</em></button></div></section>;
}
