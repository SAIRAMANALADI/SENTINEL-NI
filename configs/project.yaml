# Foundation defaults only. Values requiring dataset or official SIH confirmation
# are deliberately provisional and must not be presented as final requirements.
project:
  id: SIH26-26153
  name: network-attack-forecasting
  feature_version: provisional-v0

paths:
  dataset: data/samples/sample.csv
  model_directory: models
  results_directory: results

runtime:
  random_seed: 42
  sequence_length: 12
  forecast_horizon_k: 3
